import { Theme } from "@/shared/const/theme"

export interface JsonSettings {
    theme?: Theme
    isFirstOpen?: boolean
    settingsPageHasBeenOpened?: boolean
    articlePageHasBeenOpened?: boolean
}